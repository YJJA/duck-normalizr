import { Middleware } from "redux";
export declare const createErrorMiddleware: (stateKey: string) => Middleware;
export declare const ErrorStateKey = "global/error";
export declare const errorMiddleware: Middleware;
